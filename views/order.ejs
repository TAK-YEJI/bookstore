<!DOCTYPE html>
<html lnag="ko">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Order</title>
    <link rel="stylesheet" href="/stylesheets/cart.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">

    <script src="http://code.jquery.com/jquery-latest.min.js"></script>


</head>

<body>
    <div class="main-mypage">
        <div class="main-wrap">
            <header>
                <div class="title-mypage">
                    <h1> Book Order </h1>
                </div>
            </header>

            <link rel="stylesheet" type="text/css"
                href="//netdna.bootstrapcdn.com/font-awesome/4.1.0/css/font-awesome.min.css">
            <hr>
            <div class="row">
                <div class="col-lg-12">
                    <div class="table-responsive">
                        <table class="table user-list">
                            <thead>
                                <tr>
                                    <th><span>Book</span></th>
                                    <th><span>제목</span></th>
                                    <th><span>작가 | 출판사</span></th>
                                    <th><span>가격</span></th>
                                    <th><span>재고량</span></th>
                                    <th><span>주문수량</span></th>
                                    <th>&nbsp;</th>
                                </tr>
                            </thead>
                            <tbody>
                                <!-- <%= order_amount = 0 %> -->
                                <% for(let i=0; i < book_info.length; i++) { %>
                                    <tr>
                                        <form method="post" action="/order/orderAdd/<%= book_info[i].book_id %>">
                                            <td>
                                                <%= book_info[i].book_id%>
                                            </td>
                                            <td>
                                                <%=book_info[i].book_title%>
                                            </td>
                                            <td>
                                                <%= book_info[i].book_writer %> 지음 | <%= book_info[i].book_publisher %>
                                            </td>
                                            <td id="price_<%= i %>">
                                                <%=book_info[i].book_price%>
                                            </td>
                                            <td>
                                                <%=book_info[i].book_stock%>
                                            </td>
                                            <td><input class="amount" type="number" name="order_amount"
                                                    id="amount_<%= i %>" min="1" /></td>
                                            <tr>
                                                <td colspan="6" class="text-right"><strong>Total</strong></td>
                                                <th>&nbsp;</th>
                                                <td class="text-left"><input type="text" id="total" name="order_total" /></td>
                                            </tr>
                                    </tr>
                                <% } %>
                                        <script>
                                            var book_num = `<%=book_info.length%>`;
                                            var input = $(".amount").change(() => {
                                                var total = 0;
                                                for (let i = 0; i < book_num; i++) {
                                                    var temp_amount = $("#amount_" + i).val();
                                                    var temp_price = $("#price_" + i).text();
                                                    total += (temp_amount * temp_price);
                                                }
                                                $('#total').val(total);
                                            })
                                        </script>
                                        <thead>
                                            <tr>
                                                <th><span>카드번호</span></th>
                                                <th>&nbsp;</th>
                                            </tr>
                                        </thead>
                                        <% for(let card of card_info){ %>
                                            <td>
                                                <select class="form-select" aria-label="Default select example"
                                                    name="card_id">
                                                    <option selected name="card_id" value="<%= card.card_id %>">
                                                        <%= card.card_id %>, <%= card.card_type %>
                                                    </option>
                                                </select>
                                            </td>
                                        <% } %>
                                        <thead>
                                            <tr>
                                                <th><span>주소</span></th>
                                                <th>&nbsp;</th>
                                            </tr>
                                        </thead>
                                        <% for(let address of address_info){ %>
                                            <td>
                                                <select class="form-select" aria-label="Default select example" name="address_post">
                                                    <option selected name="address_post" value="<%= address.address_post %>">
                                                        <%= address.address_post %>, <%= address.address_default%>, <%= address.address_detail %>
                                                    </option>
                                                </select>
                                            </td>
                                        <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
        <div>
            <div class="btn-wrap">
                <button type="submit" name="btn">주문하기</button>
            </div>
        </div>
    </form>
</body>

</html>